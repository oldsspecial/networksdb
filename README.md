# NetworksDB

A Python package for network data management and processing.

## Installation

```bash
pip install -e .
```

## Usage

### Generate Network Data

```bash
generate-network-data --output network_data.csv --count 100
```

## Development

Install in development mode:

```bash
pip install -e ".[dev]"
```

Run tests:

```bash
pytest
```